{
    "lock": false,
    "fmt": {
        "lineWidth": 130,
        "indentWidth": 4,
        "proseWrap": "preserve"
    },
    "tasks": {
        "check": "deno fmt --check && deno lint && deno check **/*.ts",
        "dev": "deno fmt && deno lint && deno check **/*.ts",
        "test": "deno test --allow-env"
    }
}
